# Pending verifications — the record (opened 2026-07-09)

Standing record of everything that is BUILT but not yet live-verified, or verified-once but
awaiting its permanent-enable decision. Each item names the gate and the runbook. This file
originally rode an intentionally-open PR to stay visible; that PR was lost, and the file
sat unmerged on `docs/pending-verifications` until recovered to master on 2026-08-11
(referenced as "V11" by PR #237 and the compose flip checklist). Strike items as they
complete (with date + evidence link) rather than deleting them.

## Reception-coordinated (need a scheduled window with reception)

| # | Verification | Gate / flag | Runbook / ref |
|---|---|---|---|
| V1 | **TM30 companion mirror retest** — convergent-delta fix (e23a5ad) is UNTESTED live after the 2026-07-01 echo-loop incident: verify add mirrors once (verbatim, no title prefix), delete propagates, primary preserved, NO escalating echo across ≥2 sync ticks | `TM30_COMPANION_WRITEBACK_ENABLED` (off) | `docs/coexistence/legacy-writeback-test-runbook.md` + reception form `writeback_test` |
| V2 | **Thai-ID card text quality** — generated card's font/placement ruled "not acceptable"; fix + re-verify in iHOTEL ReportReg_1 before the permanent-enable decision | `GUEST_DOCUMENT_STORAGE_ENABLED` (on, nominally mid-test) | task8 runsheet; template ref in `thai-id-middleware-tauri/reference/` |
| V3 | **HF Hotel round-writeback Phase-1 smoke** (Ville passed 2026-07-01; HF Hotel identical code but untested) + Phase-2 reception adoption of RoundControl | `ROUND_WRITEBACK_ENABLED` (on) | `docs/coexistence/RUNBOOK-round-writeback-flip.md` |
| V4 | **OTA parked-booking promote, first live run** — park a real OTA booking roomless → assign first room → verify byte-parity CreateBooking lands in iHOTEL (#224, live since 2026-07-09) | coordinates with ota-desk `PMS_WRITEBACK_ENABLED` flip (Phase 0 = #223, merged) | `docs/pms-booking-writeback-design.md` (ota-desk) |
| V5 | **Zero-training UX gate** (ADR 0003) — per shift-loop surface as it ships, starting with the spatial grid + guest-move drag build: a receptionist who knows iHOTEL completes the task with no instruction | per-surface | ADR 0003 §acceptance; gap matrix §6b |
| V11 | **HF Hotel `/hk` end-to-end, branch-correct** (wave-4 housekeeping A). A maid opens `/hk`, taps เริ่มทำความสะอาด then เสร็จแล้ว on ONE empty room. Assert: `ht_hk_cleaning_events` rows land in `hotelnew` and **not** `hotelville`; `hotelnew.ht_rooms_new.room_clean` flips `f→t`; the `mark_room_clean` job reaches `done`; legacy `HT_Rooms.Room_Clean` for that `id` goes `'yes'→'no'`; reception's แผนกแม่บ้าน board updates **without a manual refresh** and names the maid | `HK_BRANCHES` (unset ⇒ `hfhotel`); no flag flip needed — this path is LIVE on deploy | baseline/verify SQL per `docs/coexistence/phase3-mark-dirty-runsheet.md` §"Live read path" |
| V12 | **Mark-dirty from the maid's phone** (wave-4 housekeeping B5). Set `HK_MARK_DIRTY_ENABLED=true`, redeploy, then run the EXISTING `phase3-mark-dirty-runsheet.md` (P3 baseline → dirty → **echo test at T+6..T+10, the whole point of the window** → mark clean → rollback criteria) but triggered from `/hk` instead of `/housekeeping`. Companion columns `Room_Use` / `Room_Use_Count` / `Room_Clean_Time` must be byte-identical to baseline, and **no** `HT_Housewife` row may appear (issue #276 absence check). Blast radius: one `Room_Clean` varchar on one row. The write shape is already proven — what is new is the TRIGGER (a maid's phone, unattended, able to flag an occupied room mid-stay), which is why this is not "just config". **2026-08-14: the flip is now real** — `HK_MARK_DIRTY_ENABLED` is plumbed through `docker-compose.yml` + the deploy payload (`.github/workflows/docker-build.yml`); `gh variable set HK_MARK_DIRTY_ENABLED -b true -R thehfhotel/new-hotel` + redeploy actually reaches the container (previously a silent no-op — neither var was wired past the app's own `std::env::var` read) | `HK_MARK_DIRTY_ENABLED` (default off ⇒ 403) | `docs/coexistence/phase3-mark-dirty-runsheet.md` |
| V13 | ~~**HF Ville admission to `/hk`**~~ **— DONE 2026-08-16, see Completed.** Body kept as history. HF Ville's canonical `legacy_room_id_int` WAS wrong for 30 of 34 rooms (repaired 2026-08-14; re-verified 0/34 on 2026-08-16) (legacy `HT_Rooms` interleaves `id`↔`Room_no`; observable today: legacy says Ville 203 is dirty, canonical says 205). Sequence: run `repair_room_legacy_keys --dry-run` → **check the report header names the right databases** (it prints PG `select current_database()` and `DB_SERVER:MSSQL_PORT/DB_NAME`; for Ville that must read `hotelville` and `…:1436/HOTEL` — `--apply` refuses non-zero if the PG database contradicts `SITE_ID`, but the dry run only warns) → review the before/after table → **resolve any unmatched/ambiguous rooms first**: `--apply` aborts non-zero and writes NOTHING while `Legacy without canonical` / `Canonical without legacy` / `Ambiguous legacy Room_no` are non-empty, because an unmatched canonical room keeps a stale `legacy_room_id_int` that can collide with a repaired one (`--allow-partial` overrides this — only after reading WHY each room is unmatched) → **either stop the `sync-hfville` worker for the ~1 s the apply takes, or rely on the bin's built-in re-verify** (a successful `--apply` re-reads both sides, re-plans, and exits non-zero if `Rooms still to repair` ≠ 0 — the live CT room mapper reads outside the bin's lock and can re-stamp a row microseconds after the commit; the fix is simply to re-run) → `--apply` until it exits 0 → **re-run the dry run and confirm `Rooms to repair: 0`** → restart the sync worker if it was stopped → wait two clean sync ticks → re-verify `count(*) FILTER (WHERE room_no IS DISTINCT FROM legacy_room_no) = 0` and that legacy `id=21`/`Room_no='203'` now shows `room_clean=f` on canonical `'203'` (and `'205'` returns to `t`) → only THEN add `hfville` to `HK_BRANCHES` → repeat V11/V12 at Ville. Ville has **never** run a room writeback (`writeback_jobs` has zero) — treat it as a first-deploy event. **2026-08-14: the flip is now real** — `HK_BRANCHES` is plumbed through `docker-compose.yml` + the deploy payload; `gh variable set HK_BRANCHES -b "hfhotel,hfville" -R thehfhotel/new-hotel` + redeploy actually reaches the container (previously a silent no-op). **2026-08-14, ORDERING vs V14: V14 must be green FIRST, then widen `HK_BRANCHES`, then Ville staff are usable — in that order.** Once location enforcement is on, an `HF_VILLE` employee has NO admitted branch while `HK_BRANCHES` is hfhotel-only: `/api/hk/me` answers `branches: []` + `no_location` and every room endpoint 403s. That is correct and deliberate (an unserved property must never be silently downgraded to `hfhotel` — pinned by `row12_ville_employee_has_no_branch_until_hk_branches_widens`), but it means Ville maids are locked out between the two flips. Keep that window short, or widen `HK_BRANCHES` in the SAME deploy as the enforcement flip once the legacy-key repair above is already verified.<br><br>**2026-08-16, EXECUTED — and the documented ordering was INVERTED.** `HK_BRANCHES` was widened at `10:58:28Z` (deploy run 31943085119) and `HK_LOCATION_ENFORCEMENT_ENABLED` followed at `11:05:23Z` (run 31943390719) — **widen-first, 6m55s apart**, the opposite of the "V14 green FIRST" precondition this row and V14 both state, and of the ordering pinned by `row12_ville_employee_has_no_branch_until_hk_branches_widens`. The hazard that ordering exists to prevent is the reverse-direction one: with Ville admitted and enforcement still OFF, `/api/hk/me` handed EVERY maid both properties for those ~7 minutes (plus container-restart time, backend up `11:09:46Z`), so any maid could have filed a cleaning report — or a real `MarkRoomClean` writeback — against the wrong property. **No harm: ZERO `ht_hk_cleaning_events` rows exist at `hotelville` for all time, and HF Hotel's most recent event is `07:36:58Z`, 3h21m before the window opened — no event was created at either property during or after the window.** The rule is NOT amended by this: enforcement-first remains correct and remains test-pinned; this is a recorded deviation that happened to be harmless because the window fell outside housekeeping hours | ~~`HK_BRANCHES` (add `hfville`), gated on `repair_room_legacy_keys --apply`, then on V14~~ — now `hfhotel,hfville` | `migrations/README.md` "Companion utility bins"; bin header |
| V14 | ~~**`/hk` per-employee location enforcement, first live flip**~~ **— DONE 2026-08-16, see Completed.** Body kept as history (wave-4 housekeeping C). `HK_BRANCHES` answers "which properties does this DEPLOYMENT serve"; it never answered "which property does THIS MAID work at" — so `/api/hk/me` handed every maid the same allowlist and an HF Ville maid was offered ฮาร์เบอร์ฟร้อนท์, able to file a cleaning report (and, for `done`, a real `MarkRoomClean` writeback) against the WRONG PROPERTY. HF ID holds the authoritative answer in `Employee.location`, so the backend now asks it (`POST {HFID_LOCATION_URL}` = `http://192.168.100.228:5000/api/private/reader/resolve-badge`, `X-Reader-Secret`, LAN only — **never via Cloudflare**) and serves `HK_BRANCHES ∩ {employee's own location}`, refusing any mismatched branch on every room endpoint, reads included.<br><br>**This flip can lock staff OUT — it is not "just config".** There is NO fallback by design: a NULL location, an unknown badge, an inactive/pending employee, an unreachable HF ID, an unset URL and an unset secret ALL refuse (403 for a definite no, 503 for "cannot tell"). Nothing degrades to the allowlist or to `hfhotel`, and there is no per-badge carve-out.<br><br>**Checklist, in order — do not reorder:** (1) **owner sets every maid's `Employee.location` in HF ID first** — badge **421** is the only NULL today, and it is refused the moment the flag flips; (2) `gh variable set HFID_LOCATION_URL -b "http://192.168.100.228:5000/api/private/reader/resolve-badge" -R thehfhotel/new-hotel`; (3) set the `HFID_RESOLVE_SECRET` GH **secret** to the SAME VALUE as the estate's `READER_RESOLVE_SECRET` (HF ID guards its whole app↔central surface with one secret — there is no separate credential; see `CLAUDE.md` → "Credentials & Docker secrets"); (4) redeploy and confirm the startup line reads `location_enforcement_enabled=false (lookup configured: true)` — **configured must be true BEFORE the flag flips**, or every `/hk` request 503s; (5) `gh variable set HK_LOCATION_ENFORCEMENT_ENABLED -b true -R thehfhotel/new-hotel` + redeploy; (6) verify with a real maid at each property: `/api/hk/me` returns exactly her own branch (single entry ⇒ no picker), her own branch's room list loads, and the other property's `?branch=` answers 403 — plus badge 421 (or any NULL) sees the actionable "ยังไม่ได้กำหนดสาขาของพนักงาน" panel rather than an empty picker.<br><br>**Rollback** is one variable: `gh variable set HK_LOCATION_ENFORCEMENT_ENABLED -b false` + redeploy ⇒ no lookup is issued at all and the surface is byte-identical to the pre-enforcement build. **V13 (HF Ville admission) now requires V14 green FIRST** — see the ordering note in that row.<br><br>**2026-08-16, FLIPPED.** `HK_LOCATION_ENFORCEMENT_ENABLED=true` at `11:05:23Z` (deploy run 31943390719, backend up `11:09:46Z`); `HFID_LOCATION_URL` was already set 2026-08-14. Confirmed live in the running container. **It was flipped AFTER V13's widen, not before** — see the inverted-ordering note in the V13 row for the exposure window and the evidence that it was harmless. Step (6) of the checklist above — per-property verification with a real maid at each site, including the badge-421 NULL-location panel — is **NOT covered by this record** and is the one piece still owed | ~~`HK_LOCATION_ENFORCEMENT_ENABLED` (default off ⇒ no lookup, behavior unchanged)~~ — now `true`; needs `HFID_LOCATION_URL` + `HFID_RESOLVE_SECRET` set first | `hotel-backend/src/hfid_location.rs` header; `hotel-backend/tests/test_hk_location_enforcement.rs` (the refusal matrix); peer endpoint `~/HF/fingerprint-time-logger` `app/api/reader.py` → `/resolve-badge` |
| V15 | **iHOTEL-WINS room status on `/hk`, first live read** (CR-1, wave-5 R2a — owner decision locked 2026-08-15). The maid's room list and room detail now show legacy `HT_Rooms.Room_Clean` (INVERTED: `'no'`=clean) instead of canonical `ht_rooms_new.room_clean`, which becomes the mirror. **READ-ONLY** — one `SELECT Room_no, Room_Clean FROM HT_Rooms` per list load, no transaction, no lock hint, no legacy write, so invariant #6 is not engaged and there is NO flag: it is live on deploy. Keyed on `Room_no`, never on `legacy_room_id_int` (`bin/repair_room_legacy_keys` settled that the id path is corrupt at Ville). HF Hotel reuses the legacy pool this binary already builds for the scheduler; no new env var, no payload entry.<br><br>**Verify, in order:** (1) startup line reads `/hk surface: … ihotel_room_status=["hfhotel"]` — an empty list means the reader did NOT attach and every maid is seeing the mirror; (2) open `/hk` and compare the red-dot rooms against iHOTEL's board for the same property — they must now agree room-for-room; (3) any disagreement between iHOTEL and canonical appears in the backend log as `/hk room-clean divergence` with `room_no` + both values, and appears NOWHERE on the maid's screen; (4) fallback: with legacy unreachable the list must still render 200 with every room's canonical value plus the Thai note `ขณะนี้เชื่อมต่อระบบ iHOTEL ไม่ได้ …` — never an error page. **Rollback** is a revert, not a flag; the built-in fallback is the safety valve.<br><br>**At HF Ville (after V13 only):** this is where the divergence becomes visible to the owner — legacy says Ville **203** is dirty while canonical says **205**, so a Ville `/hk` list served iHOTEL-truth will red-dot 203 where today it red-dots 205. Ville's reader is built ONLY once `HK_BRANCHES` admits `hfville`, from `VILLE_MSSQL_*` (defaults mirror the `sync-hfville` sidecar: 192.168.11.51:1436/HOTEL). Note the ORDER: fix the keys (V13) before trusting a Ville mark-clean, because the display joins on `Room_no` while the writeback still keys on `legacy_room_id_int` | none — live on deploy (the missing-reader path degrades to the canonical mirror + Thai note) | `hotel-backend/src/legacy_room_status.rs` header; `hotel-backend/tests/test_hk_ihotel_status.rs`; merge rules in `routes::hk` module docs |
| V16 | **`housekeeping_admin` — an employee who works BOTH properties** (owner-approved "admin can pick location"). V14 binds every employee to their single HF ID `Employee.location`, which is right for maids and wrong for the staff who cover both hotels. The exception is an HF ID GRANT, not a location value: `/resolve-badge` already returns the badge's full `apps` list, and `/hk` now reads it. A badge holding `housekeeping_admin` is offered the WHOLE `HK_BRANCHES` allowlist (today both properties ⇒ the existing multi-branch picker appears) and `require_branch`-passing branches all clear the location gate.<br><br>**Ships DARK-COMPATIBLE with NO flag** — the darkness IS the grant. While nobody holds it, every `/resolve-badge` answer is interpreted exactly as before and every `/hk` response is byte-unchanged (pinned by `row17_the_same_badge_without_the_grant_is_byte_unchanged`, which drives badge 421's LIVE payload). There is nothing to flip and nothing to roll back but a revert; the owner un-ticking the box is the off switch, effective within the 60s lookup cache.<br><br>**Two bounds the grant does NOT cross, both test-pinned:** (1) `HK_BRANCHES` — an admin asking for a property this deployment does not serve gets the ALLOWLIST 403, because `require_branch` runs first (`row16`); (2) `found`/`active`/`pending` — a grant on an unknown, inactive or unapproved employee still refuses, since HF ID vouching for the PERSON is a different decision from placing them at a property. It DOES override `location` entirely, **null included** — deliberately, and not a re-creation of the silent-default hole: a null location is an ABSENCE (still refused, `row17`) while the grant is an explicit tick against a named employee, auditable in HF ID.<br><br>**`housekeeping_admin` is not a way IN.** `housekeeping` is what opens the surface (Cloudflare Access policy + the `hk_access` re-check); admin-without-maid is 403 before any branch logic runs — pinned by `hk_access::admin_grant_alone_does_not_open_the_surface`, which also guards the textual trap that `"housekeeping_admin"` has `"housekeeping"` as a prefix (both comparisons are exact equality; a `starts_with` "fix" would hand the whole maid surface to every admin-only badge). The grant WIDENS branches for people who already hold `housekeeping`; it never substitutes for it. **Grant both, or the employee sees nothing.**<br><br>**Activation, owner-side, in order:** (1) the fingerprint session's catalog entry — LIVE as of their `6cfdbc32`, so `housekeeping_admin` is tickable in Employee Management now and `/resolve-badge` forwards it the moment it is granted; (2) tick it for the employee, **alongside `housekeeping`**; (3) within 60s (the positive-cache TTL) her `/hk` shows the branch picker with both properties. **Verify:** `/api/hk/me` returns BOTH branches for her and exactly ONE for an ordinary maid at each property, the picker appears for her only, and a cleaning report filed on each branch lands at the matching property (`ht_hk_cleaning_events` at `hotelnew` vs `hotelville`) | none — no flag; the HF ID grant itself is the gate. Needs the peer's catalog entry (done, `6cfdbc32`) then an owner tick | `hotel-backend/src/hfid_location.rs` header (`HK_ADMIN_GRANT`, the widen-vs-admit split); `hotel-backend/tests/test_hk_location_enforcement.rs` rows 14-19; `hotel-backend/src/middleware/hk_access.rs` (`admin_grant_alone_does_not_open_the_surface`) |

## Code-gated (fix/verify before flipping or enabling)

| # | Verification | Blocking what |
|---|---|---|
| V6 | **Checkout POS double-count landmine**: `net = cin_total_amount + product_total` double-counts once new-app POS populates `ht_pos_sales` for a stay being checked out — fix + folio-parity re-verification BEFORE using new-app POS at checkout | new-app POS at checkout |
| V7 | **Sticky-note mirror** round-trip idempotency (same echo class as V1) before `NOTES_WRITEBACK_ENABLED` | notes visible to iHOTEL shift |
| V8 | **Cash-entry outbound** — progressed 2026-08-10 (be6bbb4): recipe `execute()` + allocator + `cash_legacy_id` back-population landed, #202 echo guard CLOSED. Still unwired (no route emits the intent) and byte-shape still unverified vs `FrmAddPay.cs:638` — one coordinated capture, then wire + flip | petty-cash parity in iHOTEL reports |
| V9 | **Deposit refund on legacy-origin folios** — `cr_legacy_ds_id` backfill, then verify the WARN-no-op class is gone | deposit refunds for iHOTEL-created stays |
| V10 | **Walk-up receipt VAT attribution scope** (finance decision + verify either app's tax report against `HT_Receipt_*`) — elevated: 4.3k receipts/yr | trusting tax reports |



## Standing monitors (no action until they fire)

- Reconcile sweep: rooms + future-entity projection changes still need manual ack (customers/
  bookings/checkins self-heal; force-converge covers customers/rooms only).
- Alert playbook + incident ledger: `docs/coexistence/sync-incident-log.md`.

Done items move to the bottom with evidence:

## Completed

- ~~V11 Layout-edit writeback echo round-trip~~ — PASSED 2026-08-11 (self-service, at night):
  room 301 moved (683,704)→(683,744) via the real `PUT /api/rooms/layout` route from an
  authenticated session; writeback job 49 `move_room_tiles` succeeded; `HT_Rooms.Room_X/Room_y`
  byte-parity both directions; CT echo converged into canonical <1 min each way; ZERO
  `ht_reconcile_log` rows at any point; board restored to the exact original state.
  `LAYOUT_WRITEBACK_ENABLED=true` (GH var) is now the permanent state — จัดผัง is LIVE.

- ~~V13 HF Ville admission to `/hk`~~ + ~~V14 `/hk` per-employee location enforcement~~ — BOTH FLIPPED
  2026-08-16, as two variable-only redeploys of `eec8a25`. `HK_BRANCHES=hfhotel,hfville` set `10:58:28Z`
  (run 31943085119); `HK_LOCATION_ENFORCEMENT_ENABLED=true` set `11:05:23Z` (run 31943390719); backend
  container up `11:09:46Z` and both values confirmed in its live environment. V13's prerequisite
  legacy-key repair is verified against production:
  `count(*) FILTER (WHERE room_no IS DISTINCT FROM legacy_room_no)` = **0 of 34** at `hotelville`
  (was 30 of 34) and **0 of 58** at `hotelnew` — the bin's own correctness control.
  **Deviation on the record:** the two flips were executed WIDEN-FIRST, 6m55s apart, inverting the
  enforcement-first precondition both rows state and `row12_ville_employee_has_no_branch_until_hk_branches_widens`
  pins. For that window every maid was offered both properties. Harmless in fact — `hotelville` has
  ZERO `ht_hk_cleaning_events` rows for all time and HF Hotel's latest event is `07:36:58Z`, 3h21m
  before the window — so no report was filed against the wrong property. The documented order is
  unchanged and still applies to any future site.
  **Still owed:** V14 checklist step (6), per-property verification with a real maid at each site
  (including the badge-421 NULL-location panel). Neither flip has had that reception-coordinated check.

- ~~Checkin idempotency-gate comparator class~~ — fixed d09e756, verified live, incident logged (2026-07-06).
- ~~PR #223/#224 OTA writeback Phase 0 + parked queue~~ — merged + deployed 2026-07-09 (CI + container restart verified; live promote test = V4).
