# PG canonical ↔ legacy MSSQL cardinality map

Single source of truth for cross-system table mapping. Required by
[PROCESS.md P1 + P5](PROCESS.md). Every `ht_*` canonical table MUST have
a row here.

**Status legend:**
- ✅ — implemented and verified correct
- ⚠️ — implemented but with known divergence (cite finding)
- ❌ — missing implementation for this layer
- N/A — does not apply at this layer

**Cardinality legend:**
- `1:1` — exactly one PG row per legacy row
- `1:N` — one PG row corresponds to N legacy rows (legacy is denormalized)
- `N:1` — N PG rows per legacy row (PG is normalized)
- `N:N` — many-to-many (rare; usually via a junction)
- `merged` — PG row aggregates multiple legacy rows (e.g. header + detail collapsed)

## Domain entity tables

| PG table | Legacy counterpart | Cardinality | Source of truth | Sync mapper | Read path | Write path | Notes |
|---|---|---|---|---|---|---|---|
| `ht_customers` | `HT_Customers` | `1:1` | shared | `sync/mappers/customer.rs` | `routes/new_customers.rs` | `writeback/recipes/booking_create.rs` + walkin/checkin_to_booking | ✅ column-for-column parity (Track E2 / migration 035 — all 33 legacy columns mirrored; `cust_price_over` writeback deferred to Track G). ⚠️ EXCEPTION: `cust_dob DATE` (migration 069, check-in registration) is **PG-canonical-only** — legacy `HT_Customers` has NO date-of-birth column, so `cust_dob` is never mirrored to MSSQL and the customer sync mapper does not read it. ⚠️ Same for `cust_membership_id VARCHAR(64)` (migration 086, loyalty-app link) — PG-canonical-only, no legacy counterpart, excluded from the `UpdateCustomer` re-save and the sync mapper |
| `ht_room_types` | none (derived) | N/A | PG canonical | none | `routes/new_rooms.rs` | manual / settings | T1: verify type literals match legacy free-text where surfaced |
| `ht_rooms_new` | `HT_Rooms` | `1:1` | shared | `sync/mappers/room.rs` | `routes/rooms.rs` | maintenance + housekeeping | ✅ column-for-column parity (Track E2 / migration 036 — `Room_Use_Count` / `Room_X/Y` / `Room_Group` / `Room_Power_*` / `Room_Polity` all mirrored); ⚠️ `room_price_*` axis (weekday/weekend/special vs legacy A/B/C per customer-type) deferred to Track F |
| `ht_bookings` | `HT_Book_H` | `1:1` | shared | `sync/mappers/booking.rs` | `routes/new_bookings.rs` | `writeback/recipes/booking_create.rs` + modify/cancel | T1: stay-range derivation. Migration 076 adds PG-canonical-only OTA provenance columns `book_channel` + `book_ext_ref` (partial UNIQUE `ux_ht_bookings_channel_ext_ref WHERE book_ext_ref IS NOT NULL`) — the caller-idempotency natural key `BookingService::create` dedupes on so a double-POST of one OTA reservation can't mint two `HT_Book_H` bookings. NOT mirrored to legacy (no `HT_Book_H` counterpart column); the byte-parity `booking_create` recipe is unchanged. Migration 086 adds PG-canonical-only `book_hold_expires_at TIMESTAMPTZ` (loyalty-channel payment-hold deadline; `book_channel='loyalty'` holds are `book_status='pending'` bookings that DO ride the normal `booking_create` writeback — legacy sees them as `จอง` — and expire via the scheduler sweep through the normal `booking_cancel` writeback). |
| `ht_booking_rooms` | `HT_Book_Ds` | `N:1` (junction) | PG canonical | `sync/mappers/booking.rs` (per-line) | joined into `routes/new_bookings.rs` | `writeback/recipes/booking_create.rs` per-night INSERT | ✅ correctly modeled as junction |
| `ht_checkins` | `HT_CheckIn_H` (header) | `1:1` AT HEADER LEVEL | shared | `sync/mappers/checkin.rs::apply_checkin_aggregate` | `routes/rooms.rs`, `routes/stats.rs`, `routes/calendar.rs` (Track B3 — dashboard reads now walk `ht_checkin_rooms` junction with `cin_room_id` fallback) | walkin/checkin_to_booking recipes (Track B4 — emits one `HT_CheckIn_Ds` per junction room, single `HT_CheckIn_H` header with multi-room `Cin_Room_ALL`) | ⚠️ Track B1 (migration 043) introduced `ht_checkin_rooms` junction; Track B2 (2026-05-13) rewrote the mapper to emit per-room rows there; Track B3 (2026-05-13) migrated dashboard readers onto the junction; Track B4 (2026-05-14) closed the writeback side. `cin_room_id` column is **DEPRECATED — use `ht_checkin_rooms` junction instead**; left in place as a COALESCE fallback for pre-B2 folios until Track B5 backfill completes. **Folio room/product split (migration 079, 2026-07-28):** `cin_total_amount` mirrors `HT_CheckIn_H.Total_Price_Net`, which is **Room + Product by definition** — it is NOT a room-only basis. New nullable `cin_room_amount` mirrors `HT_CheckIn_H.Total_Price_Room` (hard overwrite, no COALESCE, compared by `existing_matches`) and is what `routes::new_checkins::folio_breakdown` uses as the room basis so `net_total = room + product` counts each POS line exactly once. NULL = never projected → falls back to `cin_total_amount`. Both columns ride the EXISTING `CHECKIN_H_PROJECTION`; no new legacy column, no legacy DDL, no new writeback statement (the checkout recipe already wrote `Total_Price_Room` — it now carries the corrected value) |
| `ht_checkin_rooms` | `HT_CheckIn_Ds` | `N:1` (junction) | PG canonical | `sync/mappers/checkin.rs::apply_checkin_aggregate` (Track B2 — emits one row per `HT_CheckIn_Ds` row in the aggregate, UPSERT keyed on `(cr_cin_id, cr_room_id)`, dropped-room DELETE cleanup) | `routes/rooms.rs` (`ROOM_PROJECTION` room_use/room_book, `get_checkouts_today_pg`, `get_room_status_pg`) + `routes/stats.rs` (`occupied_rooms`, `checkout_rooms`, `booked_rooms`) + `routes/calendar.rs` (`fetch_new_calendar_data` checkin query, per-(folio, room) synthetic id) — all join via `LEFT JOIN ht_checkin_rooms` with `COALESCE(cr.cr_room_id, cin_room_id)` fallback for pre-B2 folios. Closes Track B3 / T3 CRIT-2 + T3 HIGH-1 + T3 HIGH-3 | **Track B4** (2026-05-14) — `writeback/recipes/walkin.rs::build_statements` + `writeback/recipes/checkin_to_booking.rs::build_statements` iterate `CreateCheckInPayload.room_lines` (packed by service from junction rows) and emit **one `HT_CheckIn_Ds` INSERT per junction row** with sequential `id`s allocated under a single TABLOCKX+HOLDLOCK MAX+1 sweep. Per-room `Cin_Room_Status` Thai literals pass through verbatim. Returns `LegacyIds.checkin_ds_ids_by_room: Vec<(room_no, ds_id)>`; `bin/writeback::back_populate_legacy_ids` stamps each pair onto `ht_checkin_rooms.cr_legacy_ds_id` so subsequent edits/extends/refunds target the right legacy row | ✅ Track B1 / T1 CRIT-1 (migration 043) + Track B2 / T2 CRIT-1 (mapper rewrite) + Track B3 (dashboard read paths) + Track B4 (writeback per-room emission). Schema keyed on `(cr_cin_id, cr_room_id)` UNIQUE. Operational columns mirror the legacy `Ds` row: `cr_room_in/out`, `cr_room_status`, `cr_rate_per_night`, `cr_nights`, `cr_room_total`, deposit fields (`cr_dep_*`), `cr_cupon_count`, `cr_note`, `cr_legacy_ds_id` |
| `ht_guest_registry` | `HT_CheckIn_Other_People` | `N:1` (N companion rows per `Cin_no`; the PRIMARY guest is NOT in the legacy table — it lives on the check-in header, and canonical `guest_is_primary = true` rows come from the migration-070 registration capture, which has no legacy counterpart) | shared | `sync/mappers/guest_registry.rs::GuestRegistryMapper` (per-row CT dispatch, Track E1 / migration 022; echo-adoption UPDATE then `ON CONFLICT (guest_legacy_id) DO UPDATE`) | TM.30 reports | walkin/checkin_to_booking (companion delta writeback) | **Reconciled since Phase 6-B** (migration 081, ack cache `ht_guest_registry_legacy`): `scheduler/sync.rs::sync_guest_registry` reconciles per **FOLIO** — the whole companion set of one `Cin_no`, `legacy_pk = Cin_no` — because iHOTEL edits companions by DELETE+REINSERT (`FrmCheckIn.cs:9975`) and per-row keying would false-positive on every edit. Hash = `cin_no | <sorted "{name}|{country}" lines>`, **ids excluded**; the canonical name is re-concatenated `first [+ ' ' + last]` with the same SQL expression as the mapper's echo-adoption match. Scoped to folios whose canonical parent check-in exists AND is at/after the mirror's coverage era (no historical backfill of `ht_guest_registry`). Detection-only and SHIPPED DARK behind `RECONCILE_GUEST_REGISTRY_ARM_ENABLED`; NOT wired into either self-heal arm — repairing a folio would mean DELETING canonical companion rows from a sweep |
| `ht_room_calendar` | `HT_Room_Status` | `1:1` (per room+date) | shared | `sync/mappers/room_calendar.rs` | (deferred — current readers use ht_bookings+ht_checkins reconstruction) | writeback recipes (mark_clean / walkin / extend_stay / checkout) write to MSSQL; CT hydrates PG | ✅ Track F1 / T1 HIGH-4 — canonical per-night ledger added 2026-05-13. Read path migration deferred to follow-up track. Probed since Phase 6-C (`mirror_ht_room_calendar`, observe-only until issue #273 lands a closure path) |
| `ht_rates` | none (DEPRECATED) | N/A | PG-only legacy | none | none (POST/PUT/DELETE only) | `routes/new_rates.rs` (deprecated write-only) | ⚠️ DEPRECATED post-F4 — `(weekday/weekend/special)` axis is structurally wrong; superseded by `ht_rate_tiers`. Table left in place so the existing CRUD form does not error; removal follows once frontend retires |
| `ht_rate_tiers` | `HT_Rooms_Price` | `1:1` (composite key on `Room_Type` × `Cust_Type`) | shared | `sync/mappers/rate_tiers.rs` (periodic-poll, 15-min reconcile cadence) | `routes/new_rates.rs` (`GET /api/new/rates` + `GET /api/new/rate-tiers`) | none (iHOTEL is source-of-truth for pricing edits; writeback deferred to a later track) | ✅ Track F4 / T1 CRIT-4. Canonical pricing matrix mirrors legacy `HT_Rooms_Price` with `Room_Price` / `Room_Price_H` / `Room_Price_M` columns |
| `ht_settings` | `TB_SETTINGS` (or similar)? | TBD | TBD | TBD | various | manual | T1: verify settings sync |
| `ht_payments` | `HT_CheckIn_Pay` + `HT_Receipt_H` | `1:N` (one payment → one pay row + one receipt row)? | shared | `sync/mappers/payment.rs::ReceiptMapper` (`HT_Receipt_H` → `ht_payments`); `PaymentMapper` coalesces `HT_CheckIn_Pay` into the check-in aggregate + mirrors the per-line ledger into `ht_payment_ledger` | `routes/new_payments.rs` | `writeback/recipes/payment.rs` | T1: VAT/transfer split + multi-payment-per-checkin. **Reconciled since Phase 6-A** (migration 080, ack cache `ht_receipts_legacy`): `scheduler/sync.rs::sync_payments` hashes `receipt_no` + amount@2dp + void bit + `legacy_cin_no` keyed on **`Receipt_no`** (NOT `Pay_No` — a ledger-line pointer), scanning only receipts with a non-empty `Receipt_ref` (a no-check-in receipt is a deliberate mapper skip, `pay_cin_id` being NOT NULL). `pay_date` / `pay_method` are excluded from the hash (COALESCE-preserved / defaulted → permanent false sync lag). Detection-only and SHIPPED DARK behind `RECONCILE_PAYMENTS_ARM_ENABLED`; NOT wired into either self-heal arm, and note the one-way asymmetry that a canonical-only void (PG-only `repository/payment.rs::void`, no writeback) diverges permanently by design |
| `ht_booking_notes` | `HT_Invoice_Note`? | TBD | TBD | TBD | TBD | TBD | T1: verify |
| `ht_inventory_*` (4 tables) | `HT_CheckIn_Product`? + housekeeping/stock tables? | TBD | TBD | TBD | TBD | TBD | T1: full mapping |
| `ht_products` | `HT_Products` | `1:1` on `prod_legacy_no = Pro_no` | shared | `sync/mappers/products.rs` (periodic poll — CT enablement on `HT_Products` deferred to a sibling `migrations/legacy-mssql/` migration) | `routes/new_products.rs` | `writeback/recipes/adjust_product_stock.rs` (additive `Pro_Amt = Pro_Amt + delta` — closes stock invariant from our app's writes; legacy continues to maintain Pro_Amt for its own sales) | ✅ Track F3 / T1 CRIT-3 (migration 041) — `ht_inventory_items.inv_product_id` FK links housekeeping/POS items to canonical product master |
| `ht_maintenance_*` (2 tables) | none (likely PG-only)? | TBD | PG canonical? | none? | `routes/new_maintenance.rs` | PG-only? | T1: confirm policy |
| `ht_users` | none | N/A | PG canonical | none | auth routes | manual / admin | ✅ PG-only by design. Track G7 / migration 046 widened `role` from binary (admin/receptionist) to admin/cashier/housekeeper/receptionist and added a `display_name` column. The legacy single-role column stays in place for backwards compatibility; canonical role grants live in `ht_user_roles` |
| `ht_sessions` | none | N/A | PG canonical | none | session middleware | auth flow | ✅ PG-only by design |
| `ht_roles` | none | N/A | PG canonical | none | `middleware::permissions` (gate + cache) + `routes::auth::me` (DTO field) | manual seed (migration 046) / future admin UI | ✅ Track G7 / T4 HIGH-9. MSSQL legacy = none (iHOTEL roles are baked into the .NET binary, not a DB table). Seed grid: admin / cashier / housekeeper / receptionist |
| `ht_permissions` | none | N/A | PG canonical | none | `middleware::permissions` | manual seed (migration 046) / future admin UI | ✅ Track G7 / T4 HIGH-9. MSSQL legacy = none. Six seed keys covering Track G gated actions (`payment.refund`, `checkin.room_change`, `checkin.round_bill`, `inventory.consume`, `reports.rr4`, `admin.users`) |
| `ht_role_permissions` | none | N/A (junction) | PG canonical | none | join inside `middleware::permissions::load_permissions` | manual seed (migration 046) / future admin UI | ✅ Track G7 / T4 HIGH-9. MSSQL legacy = none. Grid follows audit T4 — admin = all six, cashier = refund/round_bill/rr4, housekeeper = consume/room_change, receptionist = room_change/consume |
| `ht_user_roles` | none | N/A (junction) | PG canonical | none | join inside `middleware::permissions::load_permissions` + `routes::auth::me` | seeded at migration time from legacy `ht_users.role`; future admin UI manages going forward | ✅ Track G7 / T4 HIGH-9. MSSQL legacy = none. A user may hold multiple rows so admin-covering-cashier-shifts compose without role swaps |
| `ht_shifts` | `HT_Round_Bill` | `1:1` (one legacy round ↔ one `ht_shifts` row, `shift_no = shift_legacy_round_id = HT_Round_Bill.id`) | shared | `bin/sync.rs::sync_round_bills` (read-only poll — **NOT** CT-enabled) | `routes/new_shifts.rs` (`GET /api/shifts/current` branch-aware) | `writeback/recipes/round_bill.rs` OpenRound/CloseRound via `ShiftService::{open_shift,close_shift}` (**SHIPPED DARK** — `ROUND_WRITEBACK_ENABLED` off); gate at `service/payment.rs::record_payment` + `service/checkin.rs::check_out` (Track G9 / migration 048 stamps `ht_checkins.cin_round_bill_shift_id`) | Track F2 / T1 HIGH-5 — migration 040 adds the canonical table + one-open-per-site partial UNIQUE index. **Round-bill coexistence (Track J6, 2026-06-26):** read-mirror LIVE — `bin/sync.rs::sync_round_bills` polls `HT_Round_Bill` (plain per-tick SELECT, **NOT** Change-Tracking-enabled) and UPSERTs `ht_shifts` per-site keyed `ON CONFLICT (shift_site_id, shift_no)` with `shift_no = shift_legacy_round_id = HT_Round_Bill.id`; both sites' open rounds verified present. Co-equal open/close writeback to legacy is **SHIPPED DARK** behind `ROUND_WRITEBACK_ENABLED` (default off, not yet enabled anywhere): `OpenRound`/`CloseRound` intents + `writeback/recipes/round_bill.rs`, emitted in-tx by `ShiftService::{open_shift,close_shift}` (OpenRound allocates `shift_no = MAX+1 = legacy id`, ledgered; legacy PK is the collision backstop vs iHOTEL's concurrent MAX+1); `POST /api/shifts/open|close` reject while the flag is off (`ShiftService` raises `ServiceError::Conflict`, surfaced as HTTP **400** today via `ApiError::BadRequest` — see `routes/new_shifts.rs`; a tighter 409 mapping is deferred. `/api/mode` exposes `roundWritebackEnabled`). Payment gate (F2) + round-bill gate (G9, migration 048) both active: each refuses unless `current_open_shift().is_some()`. G9 also adds `ht_checkins.cin_round_bill_shift_id BIGINT REFERENCES ht_shifts(shift_id)` so per-shift revenue attribution lives in canonical PG (legacy `HT_Round_Bill` / `HT_Receipt_H` have no linking column — see `docs/legacy-app/COMPAT_CHEATSHEET.md` §"Table: `HT_Round_Bill`" "**Schema** (6 cols)" and `docs/legacy-app/COMPAT_CHEATSHEET.md` §"Table: `HT_Receipt_H`" "**Schema** (20 columns)" — neither column list carries a round/shift foreign key; was a bare line-range citation (960-970) that had already drifted onto `HT_Log_Debt`) |
| `ht_payment_ledger` | `HT_CheckIn_Pay` | `1:1` per line (one legacy payment line ↔ one ledger row, keyed `ledger_legacy_id = HT_CheckIn_Pay.id`; a check-in has N lines) | shared (read-only mirror) | `sync/mappers/payment.rs::mirror_payment_ledger` (in the `PaymentMapper` coalesced path — re-mirrors all lines of a changed `Cin_No`; rides the read `load_checkin_aggregate` already issues, NOT a separate CT mapper) | `routes/new_shifts.rs::round_report` (`GET /api/shifts/{id}/report`) | none — read-only mirror (no writeback; our app's payments reach `HT_CheckIn_Pay` via `writeback/recipes/payment.rs` and CT-sync back, so the ledger holds BOTH apps' payments with no double write) | Track J7a (migration 057) — per-line mirror of the legacy payment ledger carrying every tender split (`cash`/`credit`/`free`/`tran`/`web`) + line total + category (`ledger_ds_id`, `P001`=room) + `Cin_Status`. Source for the round-close income-by-tender reconciliation + the iHOTEL-equivalent shift report (`ReportShipCash`/`ReportIncome2` numbers). Non-finite tenders coerced to 0 (review J7a P2); tender cols `NUMERIC(14,2)` for overflow headroom. Per-site (connection-level scoping, like `ht_shifts`). Known limits: a hard-DELETE of a pay line leaves a stale row (iHOTEL cancels via `Cin_Status` flip, not delete) and the anonymous-sale `Cin_No` bucket re-mirrors O(bucket) — both pre-existing in the payment sweep, perf/edge only. |
| `ht_room_changes` | `HT_Changed_Room` | `1:1` | PG canonical (shared reverse-sync) | `sync/mappers/mirror.rs::ChangedRoomMirrorMapper` (extended Track G4 — alongside the existing `legacy_mirror.ht_changed_room` projection, also upserts into canonical `ht_room_changes` keyed on `rc_legacy_id`; resolves `cin_no` + `room_no`s via FK joins, no-op when parent rows haven't yet mirrored) | (future — stay-history panel listing every room change for a folio) | `service/checkin.rs::change_room` + `writeback/recipes/room_change.rs` | ✅ Track G4 / T4 HIGH-3 (migration 045). Mid-stay room-move audit. `POST /api/new/checkins/:id/change-room` → service → outbox `WritebackIntent::RoomChange { rc_id }` → recipe `INSERT INTO [HT_Changed_Room] … OUTPUT INSERTED.id` (legacy id captured via OUTPUT clause; back-populated onto `rc_legacy_id` by writeback worker `mark_done`). PK + CT on legacy side enabled Phase 5.5b (2026-04-29). |
| `ht_coupons` | `HT_Cupon` | `1:1` | PG canonical (shared reverse-sync) | `sync/mappers/mirror.rs::CuponMirrorMapper` (extended Track G5 — dual-write via `sync/mappers/coupon.rs::apply_canonical_cupon_event` UPSERTs canonical row keyed on `legacy_cupon_no` partial UNIQUE; clears the back-pointer on legacy DELETE while preserving the canonical row for audit) | `routes/new_coupons.rs::list_coupons` (canonical) + future dashboard panel | `service/coupon.rs::{issue_coupon, redeem_coupon}` + `writeback/recipes/coupon.rs` | ✅ Track G5. Food/breakfast/promo coupon entitlement. `POST /api/new/coupons` (gated `coupon.issue`) → service → outbox `WritebackIntent::IssueCoupon { coupon_id }` → recipe `INSERT INTO [HT_Cupon]` under TABLOCKX+HOLDLOCK MAX+1 `cupon_no` allocator (back-populated onto `legacy_cupon_no` by writeback worker `mark_done`). `POST /api/new/coupons/{code}/redeem` (gated `coupon.redeem`) → service flips status → outbox `WritebackIntent::RedeemCoupon` → recipe `UPDATE HT_Cupon SET cupon_print=1 WHERE cupon_no=...`. CT on `HT_Cupon` enabled Phase 5.5b (2026-04-29). |
| `ht_pos_sales` | `HT_CheckIn_Product` | `1:N` (one folio → N sale lines) | PG canonical (shared reverse-sync) | `sync/mappers/mirror.rs::CheckinProductMirrorMapper` (extended Track G6 — alongside the existing `legacy_mirror.ht_checkin_product` opaque projection, also upserts into canonical `ht_pos_sales` keyed on `sale_legacy_id`; resolves `cin_no` → `cin_id` and `Cin_Pro_id` → `prod_id` via FK joins, no-op when parent rows haven't yet mirrored) | `routes/new_checkins.rs::list_pos_sales` (`GET /api/new/checkins/:id/pos-sales`) | `service/pos.rs::record_sale` + `writeback/recipes/pos_sale.rs` | ✅ Track G6 (migration 052). POS / sales-to-room line items. `POST /api/new/checkins/:id/pos-sale` → service validates folio + decrements `ht_products.prod_current_stock` + INSERTs sale row → outbox `WritebackIntent::RecordPosSale { sale_id }` → recipe `INSERT INTO [HT_CheckIn_Product] … OUTPUT INSERTED.id` plus a paired additive `UPDATE HT_Products SET Pro_Amt = Pro_Amt + (-qty) WHERE Pro_no=…` so the legacy stock invariant holds. Legacy id back-populated onto `sale_legacy_id` by writeback worker `mark_done`. Both CT-enabled tables involved (`HT_CheckIn_Product` since Phase 5.5b 2026-04-29; `HT_Products` CT enablement deferred — F3 mapper uses periodic poll). |
| `ht_pos_receipts` | `HT_Receipt_H` | `1:1` (one walk-up sale → one receipt header) | PG canonical | none (canonical-only outbound; no reverse-sync of standalone receipts yet) | `routes/new_pos.rs` (walk-up sale response carries the canonical receipt) | `service/pos.rs::record_walkup_sale` + `writeback/recipes/receipt.rs` | ✅ Task #45 (migration 065). POS walk-up / roomless standalone receipt header. `POST /api/new/pos/walkup-sale` → service decrements `ht_products.prod_current_stock` per line + INSERTs header + lines → outbox `WritebackIntent::RecordReceipt { receipt_id }` → recipe allocates `HT_Receipt_H.id` (TABLOCKX MAX+1) + `Receipt_no` (`B{yyMM}-{4digit}`), INSERTs `HT_Receipt_H` (`Receipt_ref=''` — no folio) + N `HT_Receipt_Ds` + a paired additive `Pro_Amt` decrement per line. Ledgered (sequential id) against crash-after-commit retries; `receipt_legacy_id`/`receipt_legacy_no` back-populated by `mark_done`. Per-site (connection-level scoping) |
| `ht_pos_receipt_lines` | `HT_Receipt_Ds` | `N:1` (one receipt header → N line items) | PG canonical | none (canonical-only outbound) | joined into the walk-up sale response | `service/pos.rs::record_walkup_sale` (canonical insert in the receipt TX) + `writeback/recipes/receipt.rs` (one `HT_Receipt_Ds` INSERT per line) | ✅ Task #45 (migration 065). Standalone receipt lines (mirror of `HT_Receipt_Ds`, `S_Sale_id = HT_Receipt_H.id`). `line_product_id` FK to `ht_products` (no cascade); `line_total = qty × unit_price − discount` |
| `ht_cash_ledger` | `TB_Pay_History` | `1:1` (per legacy pay-history line) | shared (read-only mirror; canonical→legacy writeback dark) | `bin/sync.rs::sync_cash_history` (read-only poll — **NOT** CT) | `routes/new_cash.rs` (`GET /api/cash`) | `writeback/recipes/cash_entry.rs` via `WritebackIntent::CreateCashEntry` (issue #202, migration 085 — **WIRED end-to-end** through dispatcher + `back_populate_legacy_ids`; **still UNWIRED at emission** — no route enqueues it yet, `Pay_Type`/`Pay_Group`/`Pay_Account`/`Pay_Program` byte-shape unverified, app-created entries persist canonical-only) | M3 #46 (migrations 059, 085). Petty-cash income/expense ledger. Inbound mirror live via per-tick poll; outbound writeback plumbing exists but its emission stays dark until the legacy INSERT shape is verified from the iHOTEL decompile (`FrmAddPay.cs`) and a reception-coordinated live test runs |
| `ht_cash_categories` | `TB_SET_MyType2` + `TB_SET_MyType2_2` + `TB_SET_MyType3` | `merged` (3 legacy category trees collapsed via `cat_level`) | shared (read-only mirror) | `bin/sync.rs::sync_cash_categories` (read-only poll — **NOT** CT) | `routes/new_cash.rs` (`GET /api/cash/categories`) | none (iHOTEL owns the category trees) | M3 #46 (migration 059). Category dimension for the cash ledger; `cat_level` distinguishes the three legacy tables |
| `ht_booking_products` | `HT_Book_Pro` | `N:1` (junction — one booking → N pre-ordered product lines) | PG canonical | none (canonical-only; legacy mirror deferred) | joined into `routes/new_bookings.rs` | `service/booking.rs` (canonical insert in the booking TX) — legacy `HT_Book_Pro` writeback **TODO** (INSERT shape unverified; `bp_legacy_id` + partial-unique index reserved) | M3 #52 (migration 061). Pre-ordered products on a reservation; persisted canonical-only for now |
| `ht_notes` | `HT_Room_SMS` + `HT_EMP_SMS` | `merged` (two same-shape legacy SMS tables collapsed via `note_target_kind`; `1:1` per legacy row within each kind) | shared (read-only mirror live; canonical→legacy writeback dark) | `bin/sync.rs::sync_sticky_notes` (read-only poll — **NOT** CT) | `routes/new_notes.rs` (`GET /api/notes`) | `routes/new_notes.rs::{create_note,mark_read}` (branch-aware canonical insert/update + in-tx `OutboxRepository::enqueue` on the resolved per-site pool) → `WritebackIntent::{CreateNote,MarkNoteRead}` → `writeback/recipes/sticky_note.rs` (`INSERT … OUTPUT INSERTED.SMS_ID` / `UPDATE SMS_Readed='yes'`) — **SHIPPED DARK** behind `NOTES_WRITEBACK_ENABLED` (default off) | Task #47 (migration 062). Room & staff sticky notes (โน้ตห้อง / โน้ตพนักงาน). Inbound mirror live via per-tick poll keyed `ON CONFLICT (note_target_kind, note_legacy_id)` (the two SMS tables have independent IDENTITY sequences, so the pair is the upsert key); `note_source` legacy/app; `SMS_Readed` 'yes'/'no' normalized to `note_is_read`. Outbound writeback INSERT shape verified (cheatsheet §3.22) — `SMS_ID` is IDENTITY (captured via `OUTPUT INSERTED`, back-populated onto `note_legacy_id`), CreateNote ledgered against crash-after-commit retries. Per-site (connection-level scoping) |
| `ht_verification_responses` | none | N/A | PG canonical | none | `routes/new_verification.rs` (`GET /api/verification`) | `routes/new_verification.rs::create_verification` (`POST /api/verification`, branch-aware via `state.write_pool`) | ✅ Task #58 (migration 066). In-app reception verification checklist — online equivalent of `docs/coexistence/reception-verification-TH.html`. PG-ONLY by design (legacy = none, like `ht_users`): app-internal operational data with no iHOTEL counterpart, so NO sync mapper and NO legacy writeback. Every answer stored in the `vr_answers` JSONB column keyed by question id; `vr_overall` denormalizes that checklist's section-5 overall-readiness verdict. Per-site (connection-level scoping) |
| `ht_feedback_forms` | none | N/A | PG canonical | none | `routes/new_feedback.rs` (`GET /api/feedback/forms`) | none (Tier 1 = edited via DB write/seed; admin-editor UI deferred to Tier 2) | ✅ Tier 1 data-driven feedback forms (migration 067, follow-on to #58/#71). PG-ONLY by design (legacy = none, like `ht_verification_responses`): app-internal CONFIG. `form_schema` JSONB holds the question list rendered generically by the frontend, so editing a question is a DB write (no frontend rebuild). Responses still land in `ht_verification_responses.vr_answers` (tagged `kind=form_kind`). Per-site (connection-level scoping); served from the primary pool |
| `ht_guest_documents` | `Tb_Save_Image` | `1:N` | canonical (ours) | (none) | `routes/guest_documents.rs` | `writeback/recipes/save_image.rs` | ✅ Migration 070 (check-in registration). Canonical home for guest identity documents / photos (Thai ID card chip image, passport page, webcam face photo) captured at check-in — `doc_image BYTEA` + `doc_mime`, discriminated by `doc_type` (`thai_id_card`/`passport`/`face_photo`) and `doc_source` (`chip`/`scanner`/`webcam`). `doc_type` → legacy `Tb_Save_Image.ttype`: `thai_id_card`=บัตรประชาชน, `face_photo`=รูปลูกค้า, `passport`=หนังสือเดินทาง. **ALSO populated by a sync-worker read-in poll** (`bin/sync.rs::sync_guest_documents`, dedup index migration 071): it mirrors legacy `Tb_Save_Image` → `ht_guest_documents` with `doc_source='legacy'` (read-only from legacy; idempotent UPSERT on the `doc_legacy_id` = `Tb_Save_Image.id` conflict target) so the registration form serves the SAME reconstructed Thai-ID card image iHOTEL prints — the API never touches legacy. NOT a CT mapper (low-volume per-tick poll like `sync_round_bills`); gated by kill-switch `GUEST_DOC_SYNC_ENABLED` (default ENABLED — read-only, no reception coordination). Canonical stays source of truth for app-captured docs. `doc_legacy_tmp_no` is the provisional `Tb_Save_Image.tmp_no` minted here and echoed onto the check-in POST as `photoTmpNo` — the existing check-in writeback links the legacy image row by `tmp_no` (`UPDATE Tb_Save_Image … WHERE tmp_no=`); `doc_legacy_id` = `Tb_Save_Image.id` back-populated once the mirror runs. Legacy mirror (`WritebackIntent::MirrorGuestImage` → `writeback/recipes/save_image.rs`, ledgered) is **SHIPPED DARK** behind `GUEST_DOCUMENT_STORAGE_ENABLED` (default off — documents persist canonical-only until the flag is on). Per-site (connection-level scoping) |
| `ht_hk_cleaning_events` | none | N/A | PG canonical | none | `routes/hk.rs` (`GET /api/hk/rooms`, `GET /api/hk/rooms/{id}`) + `routes/housekeeping.rs` (`GET /api/housekeeping/cleaning` — reception's live board, today's latest event per room) | `routes/hk.rs::report_cleaning` → `service::housekeeping::report_cleaning_progress` (`POST /api/hk/rooms/{id}/cleaning`, branch-aware via `state.write_pool`) | ✅ Employee-login plan Phase 4 (migration 077). Maid-reported room-cleaning progress on the `/hk` surface — append-only event log (`started`/`done`/`dirty`); latest event per room per Thai day = current progress. The TABLE itself stays PG-only (legacy = none, no sync mapper). ⚠️ **Changed 2026-08-11 (housekeeping-ops):** the `done` PHASE is no longer legacy-inert — it flips `ht_rooms_new.room_clean` and enqueues the proven `MarkRoomClean` writeback (+ `RoomMarkedClean` event) in ONE transaction, so reception sees the finished room on iHOTEL's board. `started` remains PG-only on purpose — iHOTEL's `Room_Clean_Time` feeds its room-power countdown (plan decision #3) — but now publishes the PG-only `RoomCleaningStarted` domain event so reception's แผนกแม่บ้าน board lights up live over SSE. ⚠️ **Changed 2026-08-14 (wave-4 housekeeping stream):** (a) migration **087** widens `hkev_status` to also accept `'dirty'`, which flips `room_clean` to false and enqueues the byte-pinned `MarkRoomDirty` recipe — **SHIPPED DARK** behind `HK_MARK_DIRTY_ENABLED` (default off ⇒ the route answers 403; invariant #6, the write shape is proven but a maid's phone as the TRIGGER is new); **no new writeback recipe was written** — `writeback/recipes/mark_dirty.rs` already existed and was live-verified 2026-07-29. (b) the whole tap (event row + flag flip + enqueue + domain event) now commits in ONE transaction inside `service::housekeeping::report_cleaning_progress`; it used to be a bare pool INSERT followed by a separate service transaction, which could append a duplicate row on a client retry. (c) `?branch=` is now **REQUIRED** on every `/hk` room endpoint (missing/`all`/wrong case ⇒ 400; a branch outside `HK_BRANCHES` ⇒ 403) — it used to default to HF Hotel, so a HF Ville maid's report was filed against the wrong property. `HK_BRANCHES` defaulted to `hfhotel` and **HF Ville was NOT offered** until `bin/repair_room_legacy_keys --apply` fixed `hotelville.ht_rooms_new.legacy_room_id_int` — 30/34 Ville rooms pointed at the wrong legacy room, so the first Ville room writeback would have flipped the wrong `HT_Rooms` row (see PENDING-VERIFICATIONS V13). ⚠️ **Repaired 2026-08-14, re-verified 2026-08-16 (wave-5):** `count(*) FILTER (WHERE room_no IS DISTINCT FROM legacy_room_no)` is now **0/34** at `hotelville` (and 0/58 at `hotelnew`, the bin's own correctness control); `HK_BRANCHES` is `hfhotel,hfville` and Ville IS offered. The 30/34 figure above is HISTORY — kept because it is the reason the `/hk` and reception surfaces join on `Room_no` rather than on the id pointer, which remains the correct join regardless. Repeat taps are idempotent: the conditional `WHERE room_clean IS DISTINCT FROM …` UPDATE takes the row lock, so a double-tap enqueues nothing (invariant #4). Identity = verified HF ID badge from the Cloudflare Access assertion (`middleware/hk_access.rs`); the maid's display name (badge fallback) rides the intent as `by` and lands in `HT_Housewife.h_name`. No FK to `ht_users`. Per-site (connection-level scoping). **HF Ville:** coequal writes are LIVE (`HFVILLE_WRITES_ENABLED=true` since 2026-06-29), so this path behaves identically at both properties and `HFVILLE_WRITEBACK_INTENTS` is UNSET (inert — every intent dispatches). `middleware::ville_guard` additionally exempts exactly `POST /api/hk/rooms/{id}/cleaning` from the admission gate; that is inert while the gate is open and matters only if Ville writes are turned back off, so a maid's report is not collateral damage of a front-desk write-policy toggle. Pinned by `tests/test_hk_ville_guard.rs` |
| `ht_hk_broken_reports` | none | N/A | PG canonical | none | `routes/hk.rs` (`GET /api/hk/broken-items/{id}/photo` only) | **RETIRED 2026-08-11** — `POST /api/hk/rooms/{id}/broken-items` answers `410 Gone`; no writer remains | ✅ Employee-login plan Phase 4 (migration 077). ⚠️ **Read-only history as of 2026-08-11 (housekeeping-ops):** breakage intake moved to the Housekeeping ops app (`housekeeping.thehfhotel.org/staff/report`, repo `~/HF/housekeeping`), which opens a real Work Order with photos + status lifecycle + reception ownership. The table and its existing rows are KEPT, still served by `GET /api/hk/broken-items/{id}/photo`; nothing new is written. The room-detail `reports` array and the room-list `openReports` counter were removed with the intake — a counter that can only ever decrease would have misled maids. Maid-submitted broken-item reports (a *report*, not a maintenance ticket — may be promoted to `ht_maintenance_requests` by staff later). Optional photo as `BYTEA` + mime (same storage pattern as `ht_guest_documents.doc_image`). PG-ONLY by design (legacy = none): no sync mapper, no writeback. Per-site (connection-level scoping) |
| `ht_hk_linen_reports` | none | N/A | PG canonical | none | `routes/hk.rs` (`GET /api/hk/rooms` + `GET /api/hk/rooms/{id}`) — **added after v1, which was write-only.** Every room in BOTH shapes carries `linenShortageOpen` (a boolean: this room has at least one report with `hklr_resolved_at IS NULL`, of ANY age), derived in the list's ONE statement as an `EXISTS` correlated on `r.room_id` (never an N+1) from the shared `routes::hk::linen_shortage_open_sql()` fragment. The detail response additionally carries `linenShortagesOpen`: this room's OPEN rows `GROUP BY hklr_kind` with `SUM(hklr_qty)`, emitted as `[{"kind","qty"}]` in `VALID_LINEN_KINDS` order (`[]` when none, wire codes only — Thai labels are the client's). ⚠️ **Both day-scoped fields are PRESERVED but DEPRECATED** for bundle skew: `linenShortageToday` and `linenShortages` keep their exact 088 meaning over the Bangkok civil day (`routes::hk::TODAY_BKK_LINEN`, character-for-character `TODAY_BKK` with `hkev_created_at`→`hklr_created_at`), and the migration-090 open fields are what the surface actually badges from — **completion, not midnight, is what clears a room** (the visible-until-done convention `ht_hk_room_signals` already follows). Canonical-only, so NOT scoped by `legacyStatusStale` | `routes/hk.rs::report_linen_shortage` → `service::housekeeping::report_linen_shortage` (`POST /api/hk/rooms/{id}/linen-shortage`) and `routes/hk.rs::resolve_linen_shortage` → `service::housekeeping::resolve_linen_shortages` (`POST /api/hk/rooms/{id}/linen-shortage/resolve`), both branch-aware via `state.write_pool` | ✅ Migrations 088 + 090. Maid's linen-shortage (ขาดผ้า) report on `/hk` — append-only, ONE ROW PER (submission, kind), with `hklr_report_uuid` (server-generated uuid v4) grouping one submission's rows. **NO LONGER RECORD-ONLY (migration 090, owner request 2026-09-01):** a report is OPEN until a maid marks the room restocked (เติมผ้าแล้ว). Completion is **ROOM-LEVEL** — ONE tap resolves EVERY open row for that room (`UPDATE … WHERE hklr_room_id = $1 AND hklr_resolved_at IS NULL`, one statement in one transaction, stamping `hklr_resolved_at` / `hklr_resolved_badge` / `hklr_resolved_name`) because a maid restocks a room in one trip; per-kind taps would be busywork. `hklr_resolved_at IS NULL` **is** the status — no `hklr_status` enum, so nothing can disagree with it, and every pre-090 row reads correctly as OPEN. History stays APPEND-ONLY: nothing is deleted and no filed value is rewritten, so grouping by kind still recovers consumption over any window. Only MAIDS resolve (`can_report = true`, `require_report_capability`); the reception viewer sees the backlog and is **403** `REPORT_NOT_PERMITTED_ERROR`, and `resolved: 0` is a valid idempotent success. What is UNCHANGED: the handlers commit PG rows and return — NO notification, NO Slack, **NO domain event / outbox row** (deliberately unlike ADR 0008's signals: linen is record-domain and the boards refresh by poll/reload), NO legacy writeback — and this is still not a future writeback candidate, because iHOTEL has no linen-inventory counterpart to write to, so coexistence invariant #6 is upheld by there being nothing legacy-coupled at all. `hklr_kind` is plain `TEXT` with NO CHECK enumerating kinds — the allowlist is `routes::hk::VALID_LINEN_KINDS` (`bed_sheet`, `pillowcase`, `duvet_cover`, `bath_towel`, `face_towel`, `foot_towel`) and the 400 for an unknown kind is served from it, so a new kind ships as a constant change with no migration and no binary-vs-CHECK disagreement window (the coupling 087 had to unpick for `hkev_status`). ⚠️ **`bed_sheet` (ผ้าปูที่นอน) was added exactly that way — code-only, `088_*.sql` untouched**, and it is FIRST in the constant because that array is also the display order (bed linen largest-first, mirrored by the frontend and by both `linenShortages` aggregations). `hklr_qty` is `CHECK (1..=20)` at the DB AND validated in the handler. Validation (all 400 with the repo envelope): items missing/empty, >6 entries (`MAX_LINEN_ITEMS`, pinned equal to the vocabulary size), duplicate kind, unknown kind, qty not an integer in 1..=20; the resolve endpoint takes NO body, so it has none of these. Identity = verified HF ID badge + display-name snapshot from the Cloudflare Access assertion (`middleware/hk_access.rs`) on BOTH the reporting and the resolving side; nothing identity-like is accepted from the client, no FK to `ht_users`. Same required `?branch=` gate and per-employee location gate as the cleaning route, on both endpoints. **HF Ville:** `middleware::ville_guard` exempts both paths from the admission gate alongside `POST /api/hk/rooms/{id}/cleaning` — safe because the writes are PG-only, so an admitted request can never outrun a narrowed `HFVILLE_WRITEBACK_INTENTS` (there is no intent). The resolve path is the ONE **six-segment** exemption: the matcher gained a single optional trailing segment checked against a closed `(action, sub-action)` list (`("linen-shortage", "resolve")`), never a prefix. Pinned by `tests/test_hk_ville_guard.rs`. Per-site (connection-level scoping) |
| `ht_hk_room_signals` | none | N/A | PG canonical | none | `routes/hk.rs` (`GET /api/hk/signals?branch=` — this branch's live board, `sig_status IN ('open','acked')`, oldest first; `GET /api/hk/events?branch=` — the maid's SSE stream, one `hk_signal` frame per change carrying the DTO) + `routes/housekeeping.rs` (`GET /api/housekeeping/signals?branch=` — reception's board, identical payload). Reception ALSO consumes the four `RoomSignal*` domain-event names directly on the existing `/api/events` relay | `routes/hk.rs` (`POST /api/hk/rooms/{id}/signals`, `POST /api/hk/signals/{id}/ack\|done\|cancel\|answer`) + `routes/housekeeping.rs` (`POST /api/housekeeping/rooms/{id}/signals`, `POST /api/housekeeping/signals/{id}/ack\|done\|cancel`) → `service::hk_signals::HkSignalService`; plus `service::housekeeping::report_cleaning_progress`, which auto-completes this room's live `priority_clean`/`checked_out` signals inside its OWN transaction on a เสร็จแล้ว report. All branch-aware via `state.write_pool` | ✅ ADR 0008 / migration 089. The canned reception ⇄ maid room signal — one room per signal, broadcast to the other role at that branch, lifecycle `open → acked → done`, creator's SIDE may cancel while open, visible until done whatever the day. **NO free-text column anywhere, by decision** (ADR 0008 considered and rejected "canned + optional note"). Direction is DERIVED from the caller's role and is never on the wire: `HkIdentity::can_report` → `SignalRole` (`true`=maid, `false`=the `/hk` reception viewer), and the desk surface is constantly `Desk`. A role may raise only its own direction's types (403 for the other side's — a real code this identity may not say; 400 for a code in neither vocabulary) and may ack/done/answer only the OTHER direction's, with cancel the single inversion (own direction, `open` only). Wrong-status transitions are `ServiceError::Conflict` ⇒ **400** via the repo's shared `From<ServiceError>` mapping (`ApiError::Conflict`/409 is reserved here for ship-dark FLAG refusals) — documented at `service::hk_signals`'s module header. **Two multi-row commands, each ONE transaction**: (a) a `room_check` (ขอเช็คห้อง) answer completes the check AND inserts one standing maid→desk child signal per problem (`sig_parent_id` → the check) — a guest is at the counter, so "the check closed but the มีของหาย never appeared" must be unobservable; (b) a maid's เสร็จแล้ว report closes the room's `priority_clean` + `checked_out` inside the cleaning report's transaction (`sig_done_source='clean_report'`), placed BEFORE the status branch so the sweep survives every early-return path including a tap on an already-clean room. `room_check` can NEVER be completed by a bare done tap (400 naming the answer endpoint) and is deliberately excluded from the auto-complete set — closing it on a cleaning tap would answer เคลียร์ on the maid's behalf. **PG-CANONICAL ONLY, and not a future writeback candidate**: iHOTEL has no concept of a room signal, so there is no mapper, no recipe, no `WritebackIntent`, no dark flag (invariant #6 upheld by nothing legacy-coupled existing). The four new `DomainEvent` variants (`RoomSignalRaised`/`Acked`/`Completed`/`Cancelled`, carrying the whole DTO so a board renders from the frame rather than refetching) are UI plumbing over the existing `event_log` + `pg_notify('domain_events')` fan-out; the writeback worker dispatches intents, not events. Events are keyed on the ROOM aggregate (`AggregateKind::Room`), so one room's housekeeping history stays a single `event_log` index scan alongside `RoomMarkedClean` / `RoomCleaningStarted`. `sig_type` / `sig_outcome` / `sig_done_source` are TEXT with **no CHECK** (vocabulary in `domain::hk_signal`, mirroring `app/hk/signal-vocab.ts` — ADR 0008 calls extending it "the sanctioned cheap change", so it must not need a migration; the 088 rationale); `sig_direction` and `sig_status` ARE checked because they are structural. `sig_escalated_at` is the once-only escalation stamp AND the monthly LINE-push quota ledger: `scheduler::hk_escalation` ticks every 30s, POSTs `room_check`s still `open` past 2 minutes to HF ID's `/hk-escalate` (`X-Reader-Secret` = the REUSED `HFID_RESOLVE_SECRET`), stamps on ANY 2xx including `{sent:false,reason:"nobody_on_duty"}`, and stops at `HK_ESCALATION_MONTHLY_CAP` (default 150) counted over the **Bangkok** month. **SHIPS DARK**: `HFID_ESCALATE_URL` unset ⇒ the job is not registered and that is logged once at startup — this is the ONE metered LINE path in the whole feature. Identity = verified HF ID badge + display-name snapshot from the Cloudflare Access assertion; nothing identity-like is accepted from a body, no FK to `ht_users`. **HF Ville:** `middleware::ville_guard` exempts the maid-tree writes (`POST /api/hk/rooms/{id}/signals` and `POST /api/hk/signals/{id}/{ack,done,cancel,answer}`) — safe because they are PG-only, so an admitted request cannot outrun a narrowed `HFVILLE_WRITEBACK_INTENTS` (there is no intent). The DESK twins under `/api/housekeeping/signals/*` are deliberately NOT exempt: front-desk mutations are what that toggle governs. The maid SSE stream never falls back across branches (unlike reception's `/api/events`, which degrades to HF Hotel by design) — an unwired Ville fan-out is a 503. Pinned by `tests/test_hk_ville_guard.rs` and `tests/test_hk_branch_required.rs`. Per-site (connection-level scoping) |
| `ht_hk_room_reports` | none | N/A | PG canonical | none | `routes/hk.rs` (`GET /api/hk/reports?branch=[&date=YYYY-MM-DD]` — the DAY OVERVIEW: EVERY active room of the branch in room-number order, each with its LATEST report for that Bangkok civil day or `null`, served by ONE statement (`LEFT JOIN LATERAL … ORDER BY rr_id DESC LIMIT 1`, plus a second LATERAL `COUNT(*) FILTER` for the two photo counts and, since **092**, a third for `problemCount` — never an N+1); `GET /api/hk/reports/{id}` — the FULL DTO with the tick rows, the v1-shaped `items` projection and both sides' photos with metadata). Both reads are open to BOTH roles: reception must see the maid's evidence to judge it, and the maid must see what came back with a return | `routes/hk.rs::submit_report` → `service::hk_reports::HkReportService::submit` (`POST /api/hk/rooms/{id}/report`, maid-only) and `::verify` / `::return_report` (`POST /api/hk/reports/{id}/verify|return`, reception-side only), all branch-aware via `state.write_pool` | ✅ Migration 091. **Report HK** — one maid's per-room daily attestation, digitizing the owner's paper sheet (vocabulary `app/hk/report-vocab.ts`, mirrored by `domain::hk_report`; domain language `CONTEXT.md` §Housekeeping). Since **migration 092** the checklist is PHOTO-BACKED TICKS in `ht_hk_room_report_items` (one row per item per report, all 22 every time), not exceptions. Carries the room-status code as SHE reported it (`vc`/`co`/`oo`/`so`, prefilled client-side from known room facts, overridable), `rr_all_items_ok` (**DERIVED since migration 092** — true iff no tick is a problem; the column stays and is still written, for v1 readers), 4..=24 DISTINCT maid photos and — on the verdict — 1..=4 reception photos. Lifecycle `submitted → verified | returned`; a returned report is superseded by a NEW submission carrying `rr_parent_id`, **derived server-side** (the LATEST report for that room+day, and only when it is `returned`) rather than accepted from the body, because a client-supplied parent could point at another room's report or a verified one and record a lie in the audit chain. **NO free-text column anywhere** — the return reason is canned (`not_clean` \| `items_mismatch` \| `photos_unclear`), the same decision ADR 0008 records for signals. **The role rule runs in BOTH directions and `can_report` IS the maid side**: a reception viewer is 403 `REPORT_NOT_PERMITTED_ERROR` on the submission, a maid is 403 `VERIFY_NOT_PERMITTED_ERROR` on both verdicts — including a maid who ALSO holds the `reception` grant, which is the rule ("a maid never verifies"), not a side effect. `rr_date` is the Bangkok civil day the report is FOR, **stored not derived** (a maid finishing a floor at 00:10 is still on yesterday's sheet); the client may send `date=YYYY-MM-DD` (strict, zero-padded) and the server otherwise defaults it via `chrono_tz::Asia::Bangkok` — `CURRENT_DATE` and `Utc::now().date_naive()` are both banned for `routes::hk::TODAY_BKK`'s reason. `rr_room_status` / `rr_return_reason` are TEXT with NO CHECK (app-owned vocabulary, the 088 rationale); `rr_status` IS checked because it is structural. **One `submitted` report per room per day**: the room row is locked `FOR UPDATE` as the serialization point before the probe (a lock on a not-yet-existing report row would serialize nothing), and a second submission is `ServiceError::Conflict` ⇒ **400** via the repo's shared mapping (`ApiError::Conflict`/409 stays reserved for ship-dark FLAG refusals — the convention `service::hk_signals` documents). Verdicts lock the header `FOR UPDATE` before judging, so two receptionists cannot both verify. **PG-CANONICAL ONLY and not a future writeback candidate**: iHOTEL has no Report HK counterpart, so no mapper, no recipe, no `WritebackIntent`, no dark flag (invariant #6 upheld by nothing legacy-coupled existing), and **no domain event of its own** — the overview is a poll/reload surface (linen's posture, deliberately not signals'). What the SUBMIT does publish is the **existing** `item_missing` / `item_damaged` room signals (089) it raises **inside its own transaction**, one per PROBLEM KIND present (see `ht_hk_room_report_items`). **HF Ville:** `middleware::ville_guard` exempts `POST /api/hk/rooms/{id}/report` and `POST /api/hk/reports/{id}/{verify,return}` — the verdicts included, unlike every other reception-side write, because they are served from the MAID router by a receptionist standing in the room and a 403 would strand a submitted report unjudged. Pinned by `tests/test_hk_ville_guard.rs` and `tests/test_hk_branch_required.rs`. Per-site (connection-level scoping) |
| `ht_hk_room_report_items` | none | N/A | PG canonical | none | `routes/hk.rs` (`GET /api/hk/reports/{id}` — the FULL DTO's `ticks` array, `[{item, state, qty\|null, photoId}]` in insert order, plus the v1-shaped `items` array (`[{item, problem, qty}]`) projected from the PROBLEM ticks for bundles that predate v2; the day overview deliberately omits both and carries `problemCount` + `photoCounts` instead, so one screen's payload does not grow with the property) | `service::hk_reports::HkReportService::submit` — ONE `UNNEST` statement inside the submit transaction, so "all or none" is a property of the statement rather than of a loop | ✅ Migrations 091 + **092**. **THE TICK TABLE since 092** (exceptions-only in 091, superseded the same day on the owner's *"1 picture for each tick"* directive): **ONE ROW PER ITEM PER REPORT — all 22, every time**, `UNIQUE (rri_report_id, rri_item)`. `rri_state` (`ok`\|`missing`\|`damaged`) is what the maid recorded and `rri_photo_id` is **the photo that backs it**; one photo may back several ticks (the เตียง shot covers all five bed items) — that is the speed argument, and it is why the link is a plain FK from the tick rather than a join table. The v1 biconditional (*items empty iff allItemsOk*) is GONE, replaced by a stronger rule the DB still cannot express: **the ticks are exactly the checklist** (`domain::hk_report::check_tick_coverage`), which is why it is a pure function applied by BOTH the route (before the room probe, so an incomplete checklist is a 400 with no database round-trip) and the service (it must hold for any caller). Validation (all 400 with the repo envelope): unknown item code, unknown state, an item ticked twice or never, a problem tick with no qty, an `ok` tick carrying one, qty not an integer in 1..=99, a tick with no usable `photoId`, and a DISTINCT photo total outside 4..=24 across ticks ∪ extras. `rri_qty` is NULL for `ok`; `rri_problem` is NULL for `ok` and otherwise EQUALS `rri_state`, both written from ONE value (`NULLIF(state, $6)`, the literal BOUND not interpolated) in one statement — so they cannot disagree, and the v1 `items` projection stays correct. **V1 TOLERANCE**: a row filed by 091 has `rri_photo_id IS NULL`, which is the discriminator every read uses, so such a report serves `ticks: []` and its `items` exactly as before (092 backfilled `rri_state` from `rri_problem` so a v1 exception never reads as an `ok` tick). `rri_item` is TEXT with NO CHECK: the 22-row checklist lives in `domain::hk_report::REPORT_ITEMS` mirroring `app/hk/report-vocab.ts`, and items that ARE linen reuse the EXACT `routes::hk::VALID_LINEN_KINDS` codes (pinned by a unit test) so an item problem and a ขาดผ้า report name the same thing. `rri_state` and `rri_problem` ARE checked, because the triple is structural: `TickState::problem()` → `ItemProblem::signal_type()` maps onto `item_missing` / `item_damaged`, and the submit raises **one signal per PROBLEM KIND, not per item** — `ht_hk_room_signals` has no item column and no free text (ADR 0008), so five missing items would otherwise be five byte-identical board rows for the desk to close. `rr_all_items_ok` is DERIVED from these rows. Per-site (connection-level scoping) |
| `ht_hk_room_report_photos` | none | N/A | PG canonical | none | `routes/hk.rs` (`GET /api/hk/report-photos/{id}?branch=` — the bytes, with the stored `Content-Type` and `Cache-Control: private, max-age=3600`, readable by BOTH roles; `GET /api/hk/report-photos/{id}/meta?branch=` — `{photoId, side, zone, bytes, attached, uploadedAt}`, the client's resume-after-reload probe, whose `attached` bit is what decides whether a held id may still be ticked against or deleted. The ids arrive on the FULL DTO as `ticks[].photoId` and as `photos[] {photoId, side, zone, bytes}` — both sides, with metadata — with `maidPhotoIds` / `receptionPhotoIds` kept for v1 readers, and as `photoCounts {maid, reception}` on the overview summary) | `routes/hk.rs::upload_report_photo` → `service::hk_reports::HkReportService::store_photo` (`POST /api/hk/report-photos`, **multipart/form-data**, field `photo` + optional text field `zone`, jpeg/png/webp, 5 MB cap → `{success, photoId, bytes}`), then bound by `submit` / `verify`; `routes/hk.rs::delete_report_photo` → `::delete_photo` (**`DELETE /api/hk/report-photos/{id}`**, migration 092) | ✅ Migrations 091 + **092**. `BYTEA` + mime, the 077 `ht_hk_broken_reports.hkbr_photo` storage pattern. **Every tick names one of these rows** and one photo may back several. `rrp_side` (`maid` \| `reception`) is CHECKED and **DERIVED from the uploader's role** (`PhotoSide::from_can_report`, the same boolean the two capability gates read), never sent by the client — so a maid cannot manufacture reception evidence and vice versa; both roles may therefore call the intake, the metadata read AND the delete, and no capability gate applies to any of them (ownership does the gating). `rrp_report_id` is **NULLABLE by design**: a phone uploads one picture at a time BEFORE the form is submitted, so the intake mints an UNATTACHED row and returns its id, and the submit/verify transaction binds it. **The attach statement IS the ownership check** — `UPDATE … WHERE rrp_id = ANY($2) AND rrp_report_id IS NULL AND rrp_badge = $3 AND rrp_side = $4`, with the row count compared to the requested count — so "unknown id", "someone else's photo", "the wrong side's photo" and "already attached" are one atomic verdict, and the refusal deliberately does not say WHICH condition failed. Counts: a SUBMISSION carries **4..=24 DISTINCT** photos across its ticks and extras (`REPORT_MIN_PHOTOS_TOTAL` = the four capture zones .. `REPORT_MAX_PHOTOS_TOTAL`; DISTINCT because one photo backing five ticks is one picture), a VERIFICATION 1..=4 of its own (`REPORT_MIN_PHOTOS`/`REPORT_MAX_PHOTOS`), and a RETURN none at all. `rrp_zone` is the capture zone, validated against `REPORT_ZONES` when sent and **INFORMATIONAL ONLY** — nothing joins or filters on it, and "at least one photo per zone" is a UI rule the server does not enforce (CONTEXT.md: zones are a capture ORDER, not a data-model entity); `rrp_bytes` is the stored size, denormalised because `octet_length` on a BYTEA detoasts the whole image. Two size limits: a route-local `DefaultBodyLimit` (5 MB + 64 KB of multipart framing, narrowing the router-wide 8 MB) so a hostile body is never buffered, and an explicit `MAX_REPORT_PHOTO_BYTES` check that answers a real maid a Thai 400. The content type is an ALLOWLIST, never a sniff — the bytes are served back with that exact `Content-Type`, so accepting `text/html` would make the read endpoint a stored-XSS vector on our own origin. **RETENTION: photos are KEPT FOREVER — owner decision 2026-09-02.** No purge job, no TTL, no sweeper, attached or unattached, and none may be added without a new owner decision (091's "accepted debt" is now the stated policy). The ONLY deletion path is `DELETE /api/hk/report-photos/{id}`: **uploader-only and UNATTACHED-only**, decided by ONE statement carrying the whole predicate (`rrp_id`, `rrp_badge`, `rrp_report_id IS NULL`) so a concurrent submit cannot lose the race and leave a filed report's photo deleted; a second SELECT runs only to explain a no-op — 404 unknown / **403** not yours / **400** already attached (the repo's conflict mapping). Unlike the attach, those three are distinguished, because a delete is a person tapping "remove" on one picture and "not yours" vs "already filed" send her to different places. **HF Ville:** `middleware::ville_guard` exempts `POST /api/hk/report-photos` (a whole-path equality with no id, so the READ falls through) and — its **first non-POST exemption** — `DELETE /api/hk/report-photos/{id}`, on its own arm with its own matcher so the widening is narrow on BOTH axes (this verb, this shape). Per-site (connection-level scoping) |
| `ht_guest_doc_backfill_skip` | none (internal) | N/A | PG canonical | (none) | `bin/sync.rs::sync_guest_documents` | `bin/sync.rs::mark_backfill_skipped` | Migration 072. Internal sync-state, NO legacy counterpart. Records check-ins that have no legacy `Tb_Save_Image` image so the newest-first guest-image sync-in poll advances past scan-less walk-ins instead of re-polling the same imageless check-ins forever. Not synced, not mirrored, not user-facing. `DELETE` a row to force a re-check (e.g. after a card is scanned in iHOTEL post check-in). Per-site (connection-level scoping) |

## Legacy mirror tables (read-only snapshots, no canonical equivalent)

These exist for reconciliation/observability — they mirror legacy state
1:1 without canonicalization:

| Mirror table | Legacy source | Purpose |
|---|---|---|
| `legacy_mirror.ht_cupon` | `HT_Cupon` | Coupon print history |
| `legacy_mirror.ht_checkin_product` | `HT_CheckIn_Product` | Product orders during stay |
| `legacy_mirror.ht_deposit` | `HT_Deposit` | Deposits |
| `legacy_mirror.ht_continuetime` | `HT_ContinueTime` | Short-stay extensions |
| `legacy_mirror.ht_changed_room` | `HT_Changed_Room` | Mid-stay room changes |
| `legacy_mirror.ht_rooms_cancel` | `HT_Rooms_Cancel` | Cancelled-room ledger |
| `legacy_mirror.ht_rooms_price` | `HT_Rooms_Price` | Per-room rate snapshots |
| `legacy_mirror.ht_bill_debt_h` | `HT_Bill_Debt_H` | Debt invoice header |
| `legacy_mirror.ht_bill_debt_ds` | `HT_Bill_Debt_Ds` | Debt invoice detail |
| `legacy_mirror.ht_order_up` | `HT_Order_Up` | Order ledger (up) |
| `legacy_mirror.ht_order_down` | `HT_Order_Down` | Order ledger (down) |

T4 (feature parity) should check whether any of these mirror-only tables
need to be promoted to canonical (e.g. if our app's UI is expected to
write deposits, coupons, or product orders — today only legacy writes
them).

## Operational / app-internal tables (no legacy counterpart)

| Table | Purpose |
|---|---|
| `schema_migrations` | Migration version tracking |
| `sync_status` | Per-aggregate sync watermark (newer style) |
| `legacy_ct_state` | MSSQL Change Tracking watermark (single row, `id=1`). Operational while `SYNC_PER_TABLE_WATERMARK=false` — the shipped default |
| `legacy_ct_state_per_table` | Per-table MSSQL Change Tracking watermark (migration 050, Resilience PR R3). One row per entry of `CT_ENABLED_TABLES` (19 as of 2026-07-27), so a row-lock wedge on one hot legacy table freezes only that row instead of gating every table's advance. Sibling of `legacy_ct_state`, consulted only when `SYNC_PER_TABLE_WATERMARK=true`. Rows are re-seeded from the current global watermark by migration 078 — a prerequisite for that flip, since the 050/056 `DO NOTHING` seeds left them pinned at their 2026-05-14 apply date and far outside the 2-day CT retention window |
| `legacy_sync_status` | Per-entity sync health |
| `writeback_jobs` | Outbox queue |
| `event_log` | Event sourcing log |
| `ht_reconcile_log` | Reconcile-job divergence findings |
| `ht_receipts_legacy` | Per-PK ack cache for the Phase 6-A `payments` reconcile arm (migration 080). Holds the last `mssql_hash` `scheduler/sync.rs::sync_payments` reconciled for a legacy `HT_Receipt_H.Receipt_no`, so a stable receipt short-circuits before the per-PK canonical fetch. Sibling of the (data-bearing, pre-5.5) `ht_rooms_legacy` / `ht_customers_legacy` / `ht_checkins_legacy` mirrors, but with NO data columns — it was born after the Phase 5.5 cutover, so the `LEGACY_SYNC_RECONCILE_MODE=upsert` escape hatch has nothing to mirror here and the arm is diff-only by construction. Cache ONLY — never canonical state; truncating it just re-compares every receipt next tick. Populated only while `RECONCILE_PAYMENTS_ARM_ENABLED=true` (ships DARK, compose-default false; rollout Ville → 48h soak → HF Hotel) |
| `ht_guest_registry_legacy` | Per-FOLIO ack cache for the Phase 6-B `guest_registry` reconcile arm (migration 081). Holds the last `mssql_hash` `scheduler/sync.rs::sync_guest_registry` reconciled for the companion SET of a legacy `Cin_no` — the folio, not the row, because iHOTEL's DELETE+REINSERT companion edit churns ids. No data columns (born after the Phase 5.5 cutover, so `LEGACY_SYNC_RECONCILE_MODE=upsert` has nothing to mirror). Unlike its siblings the ack suppresses ack WRITES only and never gates detection: both sides are already in memory, so a canonical-side change that leaves the legacy hash untouched stays observable — load-bearing on a TM.30 table. Cache ONLY, safe to truncate. Populated only while `RECONCILE_GUEST_REGISTRY_ARM_ENABLED=true` (ships DARK, compose-default false; rollout Ville → 48h soak → HF Hotel) |
| `ht_reconcile_era_floor` | Per-reconcile-arm scope watermark (migrations 081 + 084): `table_name` → the oldest row that arm will admit, in the ONE basis that arm uses — `era_floor` (TIMESTAMP, oldest parent time) or `era_floor_id` (BIGINT, lowest legacy IDENTITY). Written ONLY through a `GREATEST` upsert, so scope is monotonically NARROWING — a derived `MIN()` floor is a low-water mark on the PARENT row's timestamp and one historical row gaining a mirrored counterpart (iHOTEL's DELETE+REINSERT companion edit on a pre-era folio) would otherwise drag it back years and admit ~all 20,423 legacy folios instead of 830. `GREATEST` in SQL rather than Rust because the backend scheduler and `bin/sync` can tick the same database. Operators may move a floor FORWARD by hand and the clamp preserves it; deleting a row is safe (re-derived next tick). Arms: `guest_registry` (081, TIMESTAMP) and `payment_ledger_probe` (084, BIGINT). The ID basis exists because that probe's mirror is keyed on a legacy IDENTITY and its `ledger_pay_date` is `TIMESTAMPTZ` against a naive-Thai legacy side — a date floor would reintroduce the timezone reasoning the id basis avoids. It earned its ratchet live: a date-windowed `backfill_payment_ledger --days=212` mirrors WHOLE folios, and on 2026-07-30 one 2025-08 line on a monthly-billed Ville long-stay (`CH25-000076`) dragged `MIN(ledger_legacy_id)` back ~7 months, sweeping 404 never-mirrored folios into the scan as `missing_pg` (zero genuine). A date-windowed backfill and an id-derived floor are NOT interchangeable definitions of coverage when the mirror writes whole folios. The row key is each arm's own `ht_reconcile_log.table_name` / `sync_status.entity_type` literal, so the two cannot collide on the PK; per-site separation is free (each site's arm runs against its own logical PG database) |
| `ht_rr4_exports` | Track G8 / T4 CRIT-2 — audit trail for every RR.4 / ตม.30 Thai immigration foreign-guest export (one row per attempt with site, range, format, row count, exporter, SHA-256 of emitted bytes for regulator non-repudiation). PG-only; no legacy counterpart (`FrmReportRR4` in iHOTEL kept no equivalent log) |
| `ht_level_drift_alert_cooldowns` | Per-(site, table) last-alerted-at for the level-triggered reconcile-drift digest (migration 053, 2026-05-16). Replaces the process-local `Mutex<HashMap>` that was wiped on every backend restart, producing duplicate Slack alerts. PG-only — same purpose as `scheduler_notification_state` but for the drift digest instead of the per-event poller. |

## Open questions for the audits to resolve

1. **T1:** ~~Is there a `ht_checkin_rooms` junction needed?~~ RESOLVED — Track B1 (migration 043, 2026-05-13) landed the schema. Sub-waves B2 (mapper) / B3 (dashboard) / B4 (writeback) / B5 (backfill) still to come. Are there other `HT_*_Ds` legacy tables that should have PG junctions?
2. **T1:** What's the canonical model for `HT_CheckIn_Pay` + `HT_Receipt_H` + `HT_Invoice_Ds` (the payment+receipt+invoice trio)? Single `ht_payments` may collapse three legacy tables.
3. **T1:** `HT_Book_H2`, `HT_Book_Ds2` — legacy secondary tables. COMPAT_CHEATSHEET says these are unused; verify.
4. **T2:** Does every CT-tracked legacy table have a sync mapper? Cross-reference with `migrations/legacy-mssql/` PK+CT migrations.
5. **T3:** Do all dashboard queries respect cardinality? `routes/rooms.rs:49-54` definitely doesn't (multi-room blind spot). What else?
6. **T4:** Reports inventory in `docs/legacy-app/REPORTS_INVENTORY.md` — which iHOTEL reports are not implemented in our app, and is that by policy?
7. **T5:** When iHOTEL writes a room status change while our writeback is mid-recipe, what conflicts? Spike covered booking-edit concurrency; broader scenarios?
